# macOS Swift App (Deprecated)

This native Swift version has been replaced by the cross-platform Python app in `../cross-platform/`.

The cross-platform version supports macOS, Windows, and Linux with the same feature set.

See the [main README](../README.md) for installation and usage.
